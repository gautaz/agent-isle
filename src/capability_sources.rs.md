# `capability_sources.rs` — Capability source trait & collection

Defines the `CapabilitySource` trait (providing mounts and environment variables for the sandbox) and the `collect_mounts` / `collect_env` utility functions that aggregate contributions from every source.
