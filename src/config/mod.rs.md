# `config/mod.rs` — Configuration types & serde

Central config module.
Defines the main `Config`, `AgentConfig`, `MountConfig`, `SandboxConfig`, `EnvValue`, `TemplateVars`, and `ToolsConfig` types, together with their serialisation / deserialisation logic.
Re-exports all public items from sibling submodules.
