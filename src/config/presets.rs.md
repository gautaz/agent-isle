# `config/presets.rs` — Bundled agent presets

Contains pre-defined agent configurations (e.g. the `"opencode"` preset with its binary path, mounts, and lightweight arguments).
User-defined agents in the YAML file take precedence over presets.
